import React, { Component } from 'react';

export default class Instrument extends Component {
    render() {
        return (
            <div>
                <button>Make the sound!</button>
            </div>
        );
    }
}